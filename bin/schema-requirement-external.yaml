---
type: map
mapping:
    type:
        type: str
        required: true
        enum:
            - external
    title:
        type: str
        required: true
        length: {min: 1}
    link:
        type: str
        required: true
        length: {min: 1}
