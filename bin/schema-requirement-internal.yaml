---
type: map
mapping:
    type:
        type: str
        required: true
        enum:
            - internal
    topic_name:
        type: str
        required: true
        length: {min: 1}
    tutorials:
        type: seq
        required: false
        sequence:
            - type: str
              required: true
